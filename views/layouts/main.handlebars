<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{#if pageTitle}}{{pageTitle}} - {{/if}}Online Auction</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    {{{_sections.css}}}

    <style>
        body {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        
        main {
            flex: 1;
        }
        
        .navbar-brand {
            font-weight: 700;
            font-size: 1.5rem;
        }
        
        .footer {
            background-color: #212529;
            color: #fff;
            margin-top: auto;
        }
        
        .footer a {
            color: #adb5bd;
            text-decoration: none;
        }
        
        .footer a:hover {
            color: #fff;
        }

        /* Multi-level dropdown */
        .dropdown-menu .dropdown-toggle::after {
            vertical-align: middle;
            border-left: 0.3em solid;
            border-bottom: 0;
            border-top: 0;
        }
        
        .dropdown-menu .dropend .dropdown-toggle::after {
            margin-left: auto;
        }
        
        .dropdown-menu .dropdown-menu {
            left: 100%;
            top: 0;
            margin-left: 0.125rem;
        }
        
        @media (max-width: 991px) {
            .dropdown-menu .dropdown-menu {
                left: 0;
                margin-left: 1rem;
            }
        }
    </style>
</head>
<body>
    
    <!-- Header / Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="bi bi-hammer"></i> AuctionHub
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">
                            <i class="bi bi-house"></i> Home
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/products">
                            <i class="bi bi-list-ul"></i> Auctions
                        </a>
                    </li>

                    <!-- Categories -->
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="/products/" role="button" data-bs-toggle="dropdown">
                            <i class="bi bi-grid"></i> Categories
                        </a>
                        <ul class="dropdown-menu">
                            {{#if categories}}
                                {{#each categories}}
                                <li class="dropdown dropend dropdown-submenu d-flex align-items-center">
                                    
                                    <!-- Link đi trang cấp 1 -->
                                    <a class="dropdown-item flex-grow-1" href="/products/search?category={{this.id}}">
                                        {{this.cat_name}}
                                    </a>

                                    <!-- Nút mở submenu subCategories -->
                                    {{#if this.subCategories.length}}
                                    <button class="btn dropdown-toggle dropdown-toggle-split" data-bs-toggle="dropdown" aria-expanded="false"></button>
                                    {{/if}}

                                    <!-- Subcategories -->
                                    {{#if this.subCategories.length}}
                                    <ul class="dropdown-menu">
                                        {{#each this.subCategories}}
                                        <li>
                                            <a class="dropdown-item" href="/products/search?category={{this.id}}">
                                                {{this.cat_name}}
                                            </a>
                                        </li>
                                        {{/each}}
                                    </ul>
                                    {{/if}}
                                </li>
                                {{/each}}

                                <li><hr class="dropdown-divider"></li>

                                <li>
                                    <a class="dropdown-item" href="/products">
                                        <i class="bi bi-grid-3x3"></i> <strong>All Categories</strong>
                                    </a>
                                </li>
                            {{/if}}
                        </ul>
                    </li>

                </ul>
                
                <form class="d-flex me-3" role="search">
                    <div class="input-group">
                        <input class="form-control" type="search" placeholder="Search..." aria-label="Search">
                        <button class="btn btn-outline-light" type="submit">
                            <i class="bi bi-search"></i>
                        </button>
                    </div>
                </form>
                
                <ul class="navbar-nav">
                    {{#if user}}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                                <i class="bi bi-person-circle"></i> {{user.full_name}}
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li><a class="dropdown-item" href="/dashboard">
                                    <i class="bi bi-speedometer2"></i> Dashboard
                                </a></li>
                                <li><a class="dropdown-item" href="/accounts/profile">
                                    <i class="bi bi-person"></i> Profile
                                </a></li>
                                <li><a class="dropdown-item" href="/my-auctions">
                                    <i class="bi bi-bag"></i> My Auctions
                                </a></li>
                                <li><hr class="dropdown-divider"></li>
                                <li>
                                <form id="logoutForm" action="/accounts/signout" method="POST">
                                    <button type="submit" class="dropdown-item">
                                        <i class="bi bi-box-arrow-right"></i> Logout
                                    </button>
                                </form>
                                </li>
                            </ul>
                        </li>
                    {{else}}
                        <li class="nav-item">
                            <a class="nav-link" href="/accounts/signin">
                                <i class="bi bi-box-arrow-in-right"></i> Login
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="btn btn-primary ms-2" href="/accounts/signup">
                                <i class="bi bi-person-plus"></i> Register
                            </a>
                        </li>
                    {{/if}}
                </ul>
            </div>
        </div>
    </nav>

    <!-- Flash Messages -->
    {{#if messages}}
    <div class="container mt-3">
        {{#if messages.success}}
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <i class="bi bi-check-circle-fill"></i> {{messages.success}}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        {{/if}}

        {{#if messages.error}}
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="bi bi-exclamation-triangle-fill"></i> {{messages.error}}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        {{/if}}

        {{#if messages.warning}}
            <div class="alert alert-warning alert-dismissible fade show" role="alert">
                <i class="bi bi-exclamation-circle-fill"></i> {{messages.warning}}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        {{/if}}

        {{#if messages.info}}
            <div class="alert alert-info alert-dismissible fade show" role="alert">
                <i class="bi bi-info-circle-fill"></i> {{messages.info}}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        {{/if}}
    </div>
    {{/if}}

    <!-- Main Content -->
    <main class="py-4">
        <div class="container">
            {{{body}}}
        </div>
    </main>

    <!-- Footer -->
    <footer class="footer py-5">
        <div class="container">
            <div class="row">

                <div class="col-lg-3 col-md-6 mb-4">
                    <h5><i class="bi bi-hammer"></i> AuctionHub</h5>
                    <p class="text-muted">A trusted online auction platform for buying and selling safely.</p>
                    <div class="d-flex gap-2">
                        <a href="#" class="btn btn-outline-light btn-sm"><i class="bi bi-facebook"></i></a>
                        <a href="#" class="btn btn-outline-light btn-sm"><i class="bi bi-twitter"></i></a>
                        <a href="#" class="btn btn-outline-light btn-sm"><i class="bi bi-instagram"></i></a>
                        <a href="#" class="btn btn-outline-light btn-sm"><i class="bi bi-linkedin"></i></a>
                    </div>
                </div>

                <div class="col-lg-3 col-md-6 mb-4">
                    <h5>Links</h5>
                    <ul class="list-unstyled">
                        <li><a href="/about">About Us</a></li>
                        <li><a href="/how-it-works">How It Works</a></li>
                        <li><a href="/faq">FAQ</a></li>
                        <li><a href="/contact">Contact</a></li>
                    </ul>
                </div>

                <div class="col-lg-3 col-md-6 mb-4">
                    <h5>Categories</h5>
                    <ul class="list-unstyled">
                        <li><a href="/categories/electronics">Electronics</a></li>
                        <li><a href="/categories/fashion">Fashion</a></li>
                        <li><a href="/categories/home">Home & Garden</a></li>
                        <li><a href="/categories/collectibles">Collectibles</a></li>
                    </ul>
                </div>

                <div class="col-lg-3 col-md-6 mb-4">
                    <h5>Legal</h5>
                    <ul class="list-unstyled">
                        <li><a href="/terms">Terms of Service</a></li>
                        <li><a href="/privacy">Privacy Policy</a></li>
                        <li><a href="/cookies">Cookie Policy</a></li>
                        <li><a href="/guidelines">Auction Guidelines</a></li>
                    </ul>
                </div>

            </div>

            <hr class="my-4 bg-light">

            <div class="row">
                <div class="col text-center text-muted">
                    <p class="mb-0">&copy; 2025 AuctionHub. All rights reserved. | PTUDW Final Project</p>
                </div>
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
    document.addEventListener("DOMContentLoaded", () => {
        const submenus = document.querySelectorAll(".dropdown-submenu");

        submenus.forEach(menu => {
            menu.addEventListener("mouseenter", function () {
                const submenu = this.querySelector(".dropdown-menu");
                submenu.classList.add("show");
            });

            menu.addEventListener("mouseleave", function () {
                const submenu = this.querySelector(".dropdown-menu");
                submenu.classList.remove("show");
            });
        });
    });
    </script>

    {{{_sections.js}}}
</body>
</html>
